OPTIONAL SPINE EXPORT

For a real Spine Web Components character, export Bogle from Spine using the same 4.2 major/minor runtime and place:
  bogle.json (or bogle.skel)
  bogle.atlas
  bogle.png (and any additional atlas pages)

The current portfolio intentionally uses a procedural rig preview until those exported files are supplied, so the GitHub Pages site remains self-contained and does not pretend the PNG is a live Spine skeleton.
